IPTV Movistar-style - Ready package
----------------------------------
- Configured with your xTeVe URLs in backend/config.py
- This package contains a prebuilt static frontend (no node build required)
- Build and run:
    docker compose up -d --build
- Access UI at: http://<NAS-IP>:8080
